# AEI Information Hierarchy Visual Iteration Log

## Iteration 0: contract freeze

- Fixed four figures and two tables from the master prompt.
- Assigned stable Useful/Observable/Actionable semantic colors.
- Required adverse controls in Figures 2-4 rather than moving them to the
  supplement.
- Required source CSV plus SVG/PDF/300 dpi PNG for every figure.
- No visual output has been accepted before rendered QA.

## Iteration 1: evidence-complete prototypes

- Generated four source CSVs from the canonical claim authority and bound P9,
  P10, and P13 inputs.
- Exported SVG, PDF, and 300 dpi PNG with fixed metadata and editable SVG text.
- Preserved the learned task-specificity failure, matched-content controls,
  shuffled-content control, adverse feedback, and final baseline boundary in
  the main figures.
- Rejected this iteration because annotations and embedded axes overlapped.

## Iteration 2: layout repair

- Wrapped the Figure 1 evidence lanes and separated gate-transition labels.
- Reserved annotation space in Figure 2 instead of drawing text over bars or
  coordinate labels.
- Rebuilt Figure 3 panels (a) and (c) as separate diagnostic regions.
- Shortened Figure 4 display labels without changing source CSV series names.
- Added a deterministic checksum manifest for every image, source CSV, and
  caption.
- PNG visual inspection found no remaining cross-panel overlap after the final
  label-spacing pass; font and manuscript-width checks remain separately gated.
