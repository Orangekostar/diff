# MVA A5 Visual Iteration Log

## Iteration 0: preregistered structure

- Frozen three artifacts before formal target outcomes were aggregated.
- Bound every panel to registered tables and prohibited result-driven domain
  ordering, smoothing, uncertainty, and axis choices.
- Selected color-vision-safe method styles with redundant markers and lines.
- Next action: render after six-domain aggregation, inspect both formats, and
  close the QA ledger without changing the evidence contract.

## Iteration 1: formal six-domain render

- Rendered all three registered artifacts from the completed aggregate without
  changing the frozen panel map, domain order, axes, or uncertainty rules.
- Reconciled `source_data.csv`: 48 error-budget curve records, 6 held-out-domain
  effect records, and 303 training/gap records.
- Inspected all PNGs at full resolution and validated every SVG as XML; no
  clipping, blank output, illegible method collision, or misleading overlap was
  found.
- Closed the QA ledger with no result-driven visual redesign.
