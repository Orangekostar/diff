# VLM_CELL_DIAG_R1_8778aa53

Bounded diagnostic, base 8778aa53875c75f3ca681915e322a9c02cbc1ac3; existing same-branch isolated worktree is clean. User supplied full method and authorized direct execution without clarification. Planning/brainstorming ceremony is limited accordingly. Primary retains source tracing, scientific decisions, attention implementation and final review; mechanical HTML delivery can be delegated.

1. CPU export: bind q24-48 via source file hash, fit CSV/cache request, prior case/figure manifests and historical main-policy trace. No uploaded image is attached to this turn; state that limitation explicitly, while verifying the specific case named by the supplied protocol using independent file identities. Recover exact render PNG hashes. Parse cached answer and compare all 64 features; export eight images, raw text/prompt/JSON, coordinate table and synthetic roundtrip. Compare plotting rectangles against actual historical drawing function and archived overlay.
2. Inspect installed Transformers 4.49.0 Qwen implementation and processor. Preserve original dual images, template, revision, bfloat16 and SDPA. Strictly truncate at first cells numeric character. One full-prefix forward (no generate, no cache); capture last query post-RoPE Q/K from SDPA calls in decoder layers 24–27 and explicitly compute only those attention rows with FP32 softmax. Leave real SDPA output unchanged. Check row-weighted V against actual SDPA last output row. Mean all 28 heads then four fixed layers. No NxN output. At most two attempts including failures, one GPU, batch1, 900s timeout; preflight memory and running jobs first.
3. Use image_grid_thw and spatial_merge_size=2; inspect window_index reversal restoring row-major merged image tokens. Export separate clean/numbered arrays, mapping CSV, same-scale plots and attention mass across image/nonimage tokens. Record next-token mismatch without changing candidates. No full answer regeneration.
4. Mechanical static HTML links all outputs and shows truthful missing-B status. Primary opens PNG/HTML, checks the four task gates against TASK_SPEC.md, records compute once, stages scoped outputs/scripts and pushes original branch; verify HEAD/upstream/remote equality. Production src, paper and previous result outputs stay unchanged. Only one new diagnosis ledger row appended to historical compute ledger (explicit task authorization), never reset it.

No new Actor/CAI/ResNet/Reader/STOP forward, training, TEST, bootstrap or performance evaluation. No model/environment/font/full-matrix artifacts or dependency upgrades.
