# MAVIS P7 Frozen Outer Evaluation

Status: `COMPLETE`. Frozen claim tier: `Tier B`.

Aggregated MAVIS CAI AUEBC is `0.1250531822`; the strongest observed deployable baseline is `mvd_m1_o2` at `0.1249920401`. The source-selected fallback control is `0.1250485269` and the routed safe system is `0.1250488183`. MAVIS improves 2/6 held-out domains relative to the strongest deployable baseline.

All policy training, aggregation, baseline choice, confidence thresholds, and claim rules were fixed from source domains before this aggregated outer evaluation. Statistics pair physical specimens within held-out domains and weight the six domains equally.

This package supports only retrospective normalized-raster closed-loop feasibility. The external-data audit contains no method-performance evaluation and does not establish scanner-time reduction, industrial deployment, or external generalization. Missing positive controls are not claimed even when the conservative tier remains B.
